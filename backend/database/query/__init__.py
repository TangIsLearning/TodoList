"""任务查询解析层。

职责边界：
- parser：把前端传入的原始查询（结构化 dict 或旧字符串协议）解析成 ParsedQuery，
  纯函数、零数据库依赖，可独立单测。
- builder：把 ParsedQuery 翻译成 SQL WHERE 条件，是唯一懂表结构的地方。

标签 / 父任务 / 普通文本三种搜索语义只在这里定义一次，
避免在前端与后端的多个位置重复解析（历史上被解析了 4 次）。
"""
